class Repository extends Backbone.Model {
  initialize () {
  	console.log("THIS IS A REPOSITORY")
  
  	this.defaults = {
  		host: "Not specified",
  		user: "Not specified",
  		repository: "Not specified"
  	}
  }

  render() {
  	if (host == "github.com") {
  		
  	}
  }
}